.row-fluid
  .span12
    %h1.page-header
      = @domain.name
      %small Edit Alias
    
    = render :partial => 'form', locals: { mailbox_alias: @alias, domain: @domain }

    / = button_to 'Delete Account', domain_mailbox_path(@mailbox), method: :delete, class: 'btn btn-danger', confirm: "Are you sure?" unless @mailbox == current_mailbox