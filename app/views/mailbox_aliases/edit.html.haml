.row-fluid
  .span12
    %h1.page-header
      = @domain.name
      %small Edit Alias
    
    = render :partial => 'form', locals: { mailbox_alias: @mailbox_alias, domain: @domain }

    = button_to 'Delete Alias', domain_mailbox_alias_path(@mailbox_alias), method: :delete, class: 'btn btn-danger', confirm: "Are you sure?"